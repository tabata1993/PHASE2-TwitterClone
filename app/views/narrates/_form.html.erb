  <%= form_with(model: @narrate,local:true ,url: choose_new_or_edit) do |form| %>
    <div class="narate_content">
      <%= form.label :つぶやき： %>
      <%= form.text_field :content %>
    </div>
    <div class="narate_content">
      <%= form.label :画像ああ： %>
      <%= form.file_field :image %>
    </div>

    <div class="submit">
      <%= form.submit "投稿" ,:class=>"btn btn-default" %>
    </div>

  <% end %>
